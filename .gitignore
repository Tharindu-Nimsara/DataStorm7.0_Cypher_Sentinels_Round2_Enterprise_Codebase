# Data artifacts — regenerated by the pipeline, never committed.
data/
# but keep the folder structure so the pipeline has somewhere to write
!data/.gitkeep

# Python
__pycache__/

# Local-only report artifacts (figures are regenerated; genai_log is the
# old prelim scratch file — the tracked final-round log is GENAI_LOG.md).
reports/figures/
reports/genai_log.md

# AI-collaboration playbooks — kept local, not part of the public repo.
CLAUDE.md
SOLO_PLAYBOOK.md
tasks.md

# Logs and trained model binary — regenerate via the pipeline scripts.
logs/
models/
