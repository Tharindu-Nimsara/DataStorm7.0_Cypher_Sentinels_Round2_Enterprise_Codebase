pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
statsmodels>=0.14
requests>=2.31
joblib>=1.3
pyarrow>=14.0
matplotlib>=3.7
seaborn>=0.12

# ── Final-round additions ────────────────────────────────────────────
# Lower bounds match the local env we verified against (shown in comments).
scipy>=1.11          # Tobit NLL via SLSQP + budget optimiser fallback   (env 1.17.1)
shap>=0.44           # per-outlet local feature attributions for the XAI  (env 0.52.0)
lifelines>=0.27      # WeibullAFT censored-regression cross-check         (env 0.30.3)
cvxpy>=1.4           # concave budget allocation under the 5M constraint  (env 1.9.1)
streamlit>=1.30      # Outlet Intelligence web app                        (env 1.58.0)
pydeck>=0.8          # hero map layer in the app                          (env 0.9.2)
folium>=0.15         # per-outlet drill-down map (outlet + nearby POIs)   (env 0.20.0)
# XAI LLM layer talks to GitHub Models over its OpenAI-compatible REST
# endpoint using `requests` (already pinned above) — no extra SDK needed.
